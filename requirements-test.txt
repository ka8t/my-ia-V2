# =============================================================================
# MY-IA V2 — Dépendances de test
# =============================================================================
# Usage : pip install -r requirements-test.txt
# Prérequis : pip install -r requirements.txt
# =============================================================================

# Framework de test
# Note : pytest pinné à 8.3.x pour compatibilité avec pytest-playwright (qui
# borne <9). À relâcher quand pytest-playwright >=0.8 sera disponible.
pytest==8.3.4
pytest-asyncio==1.3.0
pytest-cov==4.1.0
pytest-timeout==2.2.0

# E2E
playwright==1.49.0
pytest-playwright==0.7.0

# Client HTTP
httpx==0.28.1

# JWT pour génération de tokens de test
PyJWT==2.9.0

# Lint / format / sécurité
ruff==0.7.4
mypy==1.13.0
bandit==1.7.10
